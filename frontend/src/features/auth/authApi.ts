// # All axios calls related to auth:
// │   │   │                         # login(email, password) → returns token
// │   │   │                         # register(data) → returns new user
// │   │   │                         # All calls go through lib/axiosClient.ts
// │   │   │
