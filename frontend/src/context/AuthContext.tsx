// Global state for the logged-in user
//                                 # Stores: user object, role, JWT token
//                                 # Provides: login(), logout() functions
//                                # Every component that needs the current user reads from